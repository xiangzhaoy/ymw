<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kgc.springbootymw.mapper.UserMapper">

    <select id="UserLogin" resultType="com.kgc.springbootymw.pojo.User" >
        select * from easybuy_user where loginName=#{loginname} and password=#{password}
    </select>

   <!-- <insert id="addUser" parameterType="com.kgc.springbootymw.pojo.User">
        insert into easybuy_user(loginName,userName,password,sex,identityCode,email,mobile,type)
        values (#{loginName},#{userName},#{password},#{sex},#{identityCode},#{email},#{mobile},#{type})
    </insert>-->


    <insert id="tianjia" parameterType="com.kgc.springbootymw.pojo.User">
        insert into easybuy_user(loginName,userName,password,sex,identityCode,email,mobile,type)
        values (#{loginName},#{userName},#{password},#{sex},#{identityCode},#{email},#{mobile},#{type})

    </insert>


    <select id="findAll" resultType="com.kgc.springbootymw.pojo.User">
        select * from easybuy_user
    </select>

    <delete id="dele">
        delete from easybuy_user where id = 21
    </delete>


</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kgc.springbootymw.mapper.NewMapper">

    <select id="findAll"  resultType="com.kgc.springbootymw.pojo.News">
       select  * from easybuy_news
    </select>



    <insert id="tianjia" parameterType="com.kgc.springbootymw.pojo.User">
        insert into easybuy_user(loginName,userName,password,sex,identityCode,email,mobile,type)
        values (#{loginName},#{userName},#{password},#{sex},#{identityCode},#{email},#{mobile},#{type})

    </insert>
</mapper>
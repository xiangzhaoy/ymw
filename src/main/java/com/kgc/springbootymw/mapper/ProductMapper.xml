<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kgc.springbootymw.mapper.ProductMapper">

    <select id="getProductList"   parameterType="java.lang.String" resultType="com.kgc.springbootymw.pojo.Product">
        select * from easybuy_product
        <where>
         <if test="name ! = null and name ! = ''">
              name like concat('%',#{name},'%')
         </if>
        </where>
    </select>
</mapper>